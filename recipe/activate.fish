# set where STIR installed JSON files are
set -gx STIR_CONFIG_DIR "${CONDA_PREFIX}/share/STIR-6.1/config"
set -gx STIR_DOC_DIR "${CONDA_PREFIX}/share/doc/STIR-6.1"

# echo "STIR ACTIVATE STIR_CONFIG_DIR TO $STIR_CONFIG_DIR"
